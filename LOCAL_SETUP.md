# 💻 로컬 개발 환경 설정 가이드

이 가이드는 회사 PC 외에 개인 노트북이나 다른 환경에서 프로젝트를 처음부터 설정하고 실행하는 방법을 안내합니다.

## 1. 사전 준비 (Prerequisites)

*   **Node.js**: 반드시 **v20 LTS** 버전을 설치해 주세요. (v24+ 버전은 토스 Granite 도구와 호환되지 않을 수 있습니다.)
*   **Git**: 소스 코드 복제(Clone)를 위해 필요합니다.
*   **토스 샌드박스 앱**: 모바일 테스트를 위해 스마트폰에 설치되어 있어야 합니다.

## 2. 프로젝트 내려받기 및 설치

```bash
# 1. 저장소 복제 (경로는 본인의 설정에 맞게 변경)
git clone <저장소_URL>
cd app_in_toss_influencer

# 2. 의존성 설치 (중요: --legacy-peer-deps 필수)
# 토스 디자인 시스템(TDS) 라이브러리와의 의존성 충돌을 방지합니다.
npm install --legacy-peer-deps
```

## 3. 환경 변수 설정 (.env)

프로젝트 루트에 `.env` 파일을 생성하고 필요한 정보를 입력합니다. (회사 PC의 내용을 참고하세요.)
```env
VITE_SUPABASE_URL=your_supabase_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key
```

## 4. 로컬 실행 및 테스트

### 로컬 서버 실행
```bash
npm run dev
```
브라우저에서 `http://localhost:5173`으로 접속할 수 있습니다. (이 환경에서는 **Mock 로그인**이 작동합니다.)

### 토스 샌드박스 연결
노트북과 스마트폰이 **같은 와이파이**에 연결되어 있어야 합니다.
1. `granite.config.ts` 파일의 `host` IP를 현재 노트북의 IP로 수정합니다.
2. 아래 명령어를 실행합니다.
```bash
npm run toss
```
3. 스마트폰의 샌드박스 앱에서 노트북의 IP와 8081 포트로 접속합니다.

## 5. 배포 및 QR 테스트 (CI/CD)

회사에서 설정한 것처럼 개인 노트북에서도 명령 한 줄로 배포할 수 있습니다.

### API 키 등록 (최초 1회)
```bash
npx ait token add --api-key PLQAwtbqZt0c6L0s9oBRifFpRFDxT-BAxGSX8Cjukio
```

### 빌드 및 배포 실행
```bash
# .ait 파일 생성 및 토스 서버 업로드
npx granite build && npx ait deploy
```
명령어 실행 후 터미널에 나타나는 **`intoss-private://`** 주소나 QR 코드를 통해 실제 토스 앱에서 테스트하세요.

---

### ⚠️ 주의사항
*   **Node 버전**: `node -v` 명령어로 현재 버전이 v20인지 꼭 확인하세요.
*   **로그인**: 실제 토스 앱 테스트 시, 해당 계정이 토스 개발자 센터 워크스페이스에 **멤버**로 등록되어 있어야 합니다.
